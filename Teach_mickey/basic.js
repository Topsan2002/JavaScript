// ชนิดของตัวแปร ชื่อตัวแปร = (assign ค่า) ค่าที่จะใส่
var name = 'totpsan';
let you = 'mickey';

const y = 'Only You';

let fullname = name + " " + you;

console.log(fullname, name, you);

name = "Juju";

console.log(name);
console.log(y);