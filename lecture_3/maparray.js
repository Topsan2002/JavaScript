const finalParticpants = ["Taylor", "Donald", 'Don', 'Natasha', 'Bobby'];

const announcments = finalParticpants.map((number) => {
    return number + "joined the contest";
});

console.log(announcments);